# Sample repo

This fixture intentionally mixes a mostly healthy checkout service with a shakier account service.
It is meant to exercise the scanner without making every file catastrophically wrong.
